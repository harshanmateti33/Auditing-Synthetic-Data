# config.yml
models:
  - type: main
    engine: openai
    model: meta/llama-3.1-nemotron-70b-instruct
    parameters:
      api_base: "http://localhost:8000/v1"

rails:
  input:
    flows:
      - compliance check
